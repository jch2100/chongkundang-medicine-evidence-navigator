# LOOP ENGINEERING PLAN — 의약품 근거 수집·검증·개선 루프

상태: 설계 단계 · 구현 전

## 1. 목적

MVP 20개 제품을 한 번 채우고 끝내지 않는다. 제품별 검색 성공·실패·보류·수정 이력을 남겨 다음 수집 실행의 정확도와 속도를 높인다.

```text
발견 → 수집 → 정규화 → 자동검증 → 사람 검토 → 공개 → 오류 관찰 → 규칙 개선 → 재실행
```

기준 단위는 `제품 전체`가 아니라 `제품 × 출처 × 작업`이다. 한 제품의 종근당 페이지 수집은 성공했지만 식약처 변경이력 대조는 실패할 수 있다.

## 2. 권장 MVP 오픈소스 조합

| 역할 | 도구 | 적용 방식 |
|---|---|---|
| 실행·배포 | GitHub Actions | 수동 실행, PR 검증, 필요 시 예약 실행 |
| 데이터 계약 | JSON Schema + Ajv | 제품·출처·변경·논문 필수 필드 검증 |
| 링크 검사 | lychee | 공식 URL·PubMed·PMC·출판사 링크 검사 |
| 오류 접수 | GitHub Issue Forms | 링크 오류·자료 불일치·제품 추가·연구 수정 접수 |
| 버전·감사 | Git | 데이터와 검증 로그의 변경 이력 보존 |
| 확장 선택지 | Prefect 또는 Dagster | 제품·출처·재시도·캐시·라인리지 규모가 커질 때 검토 |

GitHub Actions는 수동·이벤트·예약 실행과 실행별 로그·아티팩트를 제공한다. Ajv는 JSON Schema 기반 데이터 검증에 사용하고, lychee는 Markdown·HTML·텍스트의 깨진 링크를 검사한다. GitHub Issue Forms는 실패 접수를 정해진 필드로 표준화한다.

## 3. 실행 단위와 상태

모든 실행에 고유한 `run_id`를 부여한다.

```text
RUN-2026-09-21-0001
```

### 제품별 작업 상태

```text
queued → collecting → normalized → automated_check → human_review → published
```

실패 또는 보류 시 다음 상태로 이동한다.

```text
retryable_failure  일시적 네트워크·링크 오류
blocked            출처 불일치·허가권자 불명·문서 접근 불가
rejected           관련성 부족·공개 불가·허가 외 해석
stale              재확인 기한 초과
```

`published`가 아닌 자료는 공개 데이터에 포함하지 않는다.

## 4. 성공 로그

성공을 “스크립트가 끝났다”로 정의하지 않는다. 필드와 근거가 확인되어야 성공이다.

```json
{
  "run_id": "RUN-2026-09-21-0001",
  "product_id": "product-example",
  "task": "ckd_product_profile",
  "source_id": "ckd-product-page-example",
  "status": "success",
  "fields_confirmed": ["ingredient", "strength", "dosage_form", "indications"],
  "source_url": "https://example.org/source",
  "retrieved_at": "2026-09-21T09:00:00+09:00",
  "review_status": "published",
  "reviewer": "확인 필요",
  "notes": "제품코드와 함량 일치"
}
```

제품별 성공률보다 `필드별 근거 확보율`을 우선 지표로 삼는다.

## 5. 실패 로그

실패는 삭제하지 않고 원인과 다음 행동을 남긴다.

```json
{
  "run_id": "RUN-2026-09-21-0001",
  "product_id": "product-example",
  "task": "mfds_change_history",
  "status": "blocked",
  "failure_code": "DATE_CONFLICT",
  "source_url": "https://example.org/source",
  "observed": "게시일과 적용일이 다르고 적용일 원문을 확인하지 못함",
  "impact": "변경 타임라인 공개 보류",
  "next_action": "식약처 원문 또는 허가권자 변경대비표 재확인",
  "owner": "확인 필요",
  "retry_after": "manual_review",
  "resolved": false
}
```

## 6. 실패 분류 체계

| 코드 | 의미 | 자동 재시도 | 공개 처리 |
|---|---|---:|---|
| `SOURCE_UNAVAILABLE` | 일시적 접속 실패 | 예 | 이전 검토본 유지 |
| `LINK_BROKEN` | URL 오류·리디렉션 실패 | 1회 후 수동 | 신규 공개 중지 |
| `NO_MATCH` | 제품·성분 매칭 실패 | 아니오 | 후보 보류 |
| `MULTIPLE_MATCHES` | 동일명·함량·제형 중복 | 아니오 | 사람 확인 |
| `SCHEMA_ERROR` | 필수 필드·자료형 오류 | 아니오 | 배포 차단 |
| `DATE_CONFLICT` | 게시일·적용일·확인일 충돌 | 아니오 | 변경정보 공개 보류 |
| `OWNER_CONFLICT` | 판매자·허가권자 불일치 | 아니오 | 공동판매 태그 확인 |
| `LITERATURE_IRRELEVANT` | 논문 관련성 부족 | 아니오 | 후보 제외 사유 기록 |
| `ABSTRACT_AMBIGUOUS` | 초록만으로 결론 불명확 | 아니오 | HCP 요약 보류 |
| `LICENSE_UNKNOWN` | 공개·재배포 조건 불명 | 아니오 | 원문 저장 금지, 링크만 검토 |
| `STALE` | 재확인 기한 초과 | 수시 | 최신성 경고 |

## 7. 루프 실행 방식

1. 수집 대상 제품 또는 출처를 선택한다.
2. 제품별 검색어 사전과 이전 실패 로그를 불러온다.
3. 공식 출처·PubMed 후보를 수집한다.
4. 원문 URL·문서일·제품코드·성분·함량을 정규화한다.
5. JSON Schema 검증을 실행한다.
6. 링크 검사를 실행한다.
7. 성공·실패·보류 로그를 생성한다.
8. 사람 검토가 필요한 항목만 검토 큐에 넣는다.
9. 검토 완료된 항목만 공개 데이터에 반영한다.
10. 실행 요약을 커밋 또는 아티팩트로 저장한다.

### 재실행 원칙

- 이미 `published`이고 출처·내용·기준일이 변하지 않으면 재작성하지 않는다.
- 일시적 접속 실패만 자동 재시도한다.
- 내용 충돌·허가권자 불일치·연구 관련성 판단은 자동 재시도하지 않는다.
- 실패 원인이 해결되지 않은 채 같은 작업을 무한 반복하지 않는다.
- 동일 출처를 반복 요청하지 않도록 캐시·ETag·마지막 확인일을 활용한다.

## 8. 성공·실패 기록 보관 위치

```text
qa/
├─ runs/
│  └─ 2026-09-21_RUN-2026-09-21-0001.json
├─ failures.jsonl
├─ corrections.md
├─ link-check/
└─ screenshots/
```

실행 로그는 GitHub Actions에만 두지 않는다. Actions 아티팩트는 실행 결과 공유에 사용하고, 개인정보·내부정보를 제거한 실행 요약은 Git 이력에도 남긴다.

## 9. 검색 품질 개선 루프

매 실행 후 다음을 갱신한다.

- 검색에 성공한 제품명·성분명·별칭
- 검색 실패한 표현
- 잘못 매칭된 제품명
- 자주 누락되는 질병명·MeSH 용어
- 같은 성분으로 잘못 연결된 함량·제형
- 관련성이 낮아 제외한 논문과 제외 사유
- 반복적으로 실패하는 공식 URL
- 사람이 수정한 필드와 수정 원인

이 기록을 다음 실행의 별칭 사전·검색식·검증 규칙에 반영한다.

## 10. 검증 지표

파일럿에서는 목표 수치를 먼저 정하지 않고 기준선을 만든다.

- 제품별 완료율
- 핵심 필드 근거 확보율
- 자동검증 통과율
- 링크 정상률
- 사람 검토 대기 건수
- 논문 후보 대비 공개 승인률
- 반복 실패율
- 오래된 자료 비율
- 사용자 오류 신고와 수정 완료 시간
- 검색 실패 표현 수

## 11. 확장 단계

### 1단계 — MVP

GitHub Actions와 로컬 스크립트만 사용한다. 20개 제품에는 이 구조가 가장 단순하고 재현 가능하다.

### 2단계 — 수집량 증가

제품 수·출처·논문 수가 늘어 수동 실행과 로그 관리가 복잡해지면 Prefect를 검토한다. Prefect는 Python 기반 흐름에 재시도·캐시·상태·실행 관찰을 추가할 수 있다.

### 3단계 — 자산·라인리지 관리

제품·출처·변경·논문을 데이터 자산으로 관리하고 의존관계와 신선도 검사를 시각화할 필요가 생기면 Dagster를 검토한다. Dagster Asset Checks는 데이터 품질·신선도 검사를 자산과 연결하고 실패 시 후속 처리를 막을 수 있다.

## 12. 루프 게이트

- [ ] 모든 실행에 run_id가 있는가?
- [ ] 성공과 실패가 제품·작업·출처 단위로 구분되는가?
- [ ] 실패 사유와 다음 행동이 있는가?
- [ ] 자동 재시도 가능한 오류와 사람 판단 오류가 구분되는가?
- [ ] 실패 로그가 다음 검색식·별칭·검증 규칙에 반영되는가?
- [ ] 검토 전 데이터가 공개 데이터와 분리되는가?
- [ ] 배포 전 스키마·링크·출처 검사가 통과하는가?
- [ ] 이전 실행과 현재 실행을 비교할 수 있는가?
